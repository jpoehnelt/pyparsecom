============
Installation
============

At the command line::

    $ easy_install pyparsecom

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pyparsecom
    $ pip install pyparsecom
